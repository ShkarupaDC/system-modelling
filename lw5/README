## How to view and run

You can view Petri Networks using the GUI application from the following [GitHub repository](https://github.com/StetsenkoInna/PetriObjModelPaint). First, open a .pns file by selecting `File` and then `Open`. Second, run a simulation by clicking on `Run` and then `run`. Simulation parameters are placed on the status bar at the bottom of the application window.
